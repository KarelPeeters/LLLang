//before
fun %main() {
  <entry>
    jump <while.cond>
  <while.cond>
    %x2 i32 = phi [<entry>: 5 i32, <if.then>: 6 i32, <if.else>: %x2 i32]
    branch 1 i1 <while.body> <while.end>
  <while.body>
    %2 i1 = neq %x2 i32, 5 i32
    branch %2 i1 <if.then> <if.else>
  <if.then>
    jump <while.cond>
  <if.else>
    jump <while.cond>
  <while.end>
    eat(%x2 i32)
    return
}

//after
fun %main() {
  <entry>
    jump <while.cond>
  <while.cond>
    branch 1 i1 <while.body> <while.end>
  <while.body>
    branch 0 i1 <if.then> <if.else>
  <if.then>
    jump <while.cond>
  <if.else>
    jump <while.cond>
  <while.end>
    eat(5 i32)
    return
}
